name: Fixture Verification

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install orchestrator dependencies
        run: pip install -r fixture_verifier/requirements.txt

      - name: Run unit tests
        run: |
          python fixture_verifier/tests/test_classifier.py
          python fixture_verifier/tests/test_fixture_gen.py

      - name: Run fixture verification pipeline
        working-directory: fixture_verifier
        run: python orchestrator/verify.py

      - name: Upload verification report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: verification-report
          path: fixture_verifier/reports/
